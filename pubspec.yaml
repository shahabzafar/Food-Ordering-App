name: food_ordering_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Database and path dependencies
  sqflite: ^2.2.5+1
  path: ^1.9.0
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Updated version for linting rules

flutter:
  uses-material-design: true

  # Adding the assets folder for images or other files
  assets:
    - assets/
